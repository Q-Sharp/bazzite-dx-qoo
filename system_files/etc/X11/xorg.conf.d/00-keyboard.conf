# Managed by bazzite-dx-qoo image build. Do not use localectl on this host,
# it will fork this file into local /etc state and shadow the image version.
Section "InputClass"
    Identifier "system-keyboard"
    MatchIsKeyboard "on"
    Option "XkbLayout" "de"
    Option "XkbModel" "pc105"
    Option "XkbOptions" "caps:shiftlock"
EndSection
